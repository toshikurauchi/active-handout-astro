<script src="./inject-copy-button.ts"></script>

<style lang="scss" is:global>
  $top-offset: 0.5em;
  $right-offset: 0.2em;

  pre {
    position: relative;
  }

  .code-clipboard-btn {
    --scroll-offset: 0px;

    background-color: transparent;
    border: none;
    position: absolute;
    top: $top-offset;
    right: calc($right-offset - var(--scroll-offset, 0));
    cursor: pointer;
    opacity: 0.1;

    &:hover {
      opacity: 1;
    }

    img {
      width: 1.5rem;
      height: 1.5rem;
    }
  }

  .code-clipboard-tooltip {
    display: none;
    position: absolute;
    top: calc($top-offset + 1.5rem + 0.5em);
    right: $right-offset;
    background-color: #eee;
    padding: 0.3em 0.5em;
    font-size: 1rem;
    border-radius: 0.2em;

    &.show {
      display: block;
    }
  }
</style>
