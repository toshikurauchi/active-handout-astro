---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {
  primary?: boolean;
}

const { primary, ...props } = Astro.props;
---

<button class:list={[{ primary: primary }]} {...props}>
  <slot />
</button>

<style lang="scss">
  @use "./styles";
</style>
